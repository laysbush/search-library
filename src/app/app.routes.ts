import { Routes } from '@angular/router';
import {TableSortComponent} from './components/table-sort/table-sort.component';

export const routes: Routes = [
  { path: '', component: TableSortComponent },
];
